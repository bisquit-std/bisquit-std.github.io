import React from "react";

export default function About() {
    return (
        <section
            id="about"
            className="ml-64 flex flex-col min-h-screen items-center justify-center bg-secondary"
        >
            <p className="text-2xl font-medium">about</p>
        </section>
    );
}
